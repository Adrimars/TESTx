import { Card, CardContent, CardHeader, CardTitle } from "@testx/ui";

export default function UsersPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Evaluators</CardTitle>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">User list API will be implemented in Phase 5.</CardContent>
    </Card>
  );
}
